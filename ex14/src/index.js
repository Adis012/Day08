var a = parseInt("03");

function convertStrToInt(parseInt) {
    String = convertStrToInt;
    return convertStrToInt;
}

// Only change code below this line

// Only change code above this line
module.exports = convertStrToInt;